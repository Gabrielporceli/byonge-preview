import type { FC } from 'react';

const TaglineStrip: FC = () => {
  return (
    <section className="tagline-strip">
      <div className="container">
        <p className="tagline">Além do essencial.</p>
      </div>
    </section>
  );
};

export default TaglineStrip;
